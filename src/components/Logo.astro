---
import { Image } from "astro:assets"

import logo from '../assets/logo.png'
---

<div class="flex flex-col items-center group mt-[10px]">
    <Image src={logo} alt="Logo" quality="high" width={180} height={180} class="absolute mt-[8px] object-cover opacity-[0.15] transition-opacity duration-1000 blur-[2rem] z-[-10] group-hover:opacity-[0.45]"/>
    <Image src={logo} alt="Logo" quality="high" width={160} height={160} class="object-cover transition-transform duration-300 ease-in-out mt-[1.2rem] hover:scale-[1.05]"/>
</div>